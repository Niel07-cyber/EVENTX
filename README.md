# EVENTX

A full-stack event management application.

## Structure

- `frontend/`: React/TypeScript application (Vite)
- `backend/`: Node.js/Express API (TBD)

1. Create a terraform.tfvars file in the terraform folder
2. Copy this and put it in the file:
   - subscription_id = "PUT-YOUR-REAL-ID-HERE"
   - sql_admin_password = "YourStrongPassword123!"
